appId: com.solunivo.app
name: Wait two seconds (shared) — Maestro has no sleep; an optional wait for nothing is one
---
- extendedWaitUntil:
    visible: 'solunivo-live-never-visible'
    timeout: 2000
    optional: true
