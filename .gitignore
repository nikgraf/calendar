node_modules/
dist/
dist-electron/
out/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

# Brand studies, PNG export sets and downloadable kits are regenerated.
output/branding/

# Expo / native (continuous native generation — prebuild output stays untracked)
apps/ios/.expo/
apps/ios/ios/
apps/ios/android/
# Downloaded EAS dev client and the live sidecar's state (e2e/ci, scripts/google-live-scratch.ts)
apps/ios/build/
google-oauth.local.json
# The live Google suite's refresh token (scripts/google-live-token.mjs --write).
google-live.local.json
# Written by CI from secrets for the testing build (see docs/distribution.md).
apps/desktop/google-oauth.json
apps/desktop/out/
# CI-only overlay (apps/ios/e2e/ci/app.config.ci.cjs is the source)
apps/ios/app.config.js

# Claude Code: per-machine permissions and session checkpoints stay local
.claude/settings.local.json
.claude/RESUME.md
